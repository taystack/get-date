import { PossibleConstructors } from '../types';
/**
 * @returns Date[] from PossibleConstructors[] (Date, string, number, undefined)
 */
export declare const getDates: (...dates: PossibleConstructors[]) => Date[];
