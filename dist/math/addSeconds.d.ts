import { PossibleConstructors } from '../types';
/**
 * @param seconds: number of second segments to add
 * @returns new MathDate()
 */
export declare const addSeconds: (date: PossibleConstructors, seconds?: number) => Date;
