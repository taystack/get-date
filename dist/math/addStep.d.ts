import { Step, PossibleConstructors } from '../types';
/**
 * @returns Date with count * Step segments added
 */
export declare const addStep: (date: PossibleConstructors, step?: Step, count?: number) => Date;
