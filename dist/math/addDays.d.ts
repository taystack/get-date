import { PossibleConstructors } from '../types';
/**
 * @param days: number of day segments to add
 * @returns new MathDate()
 */
export declare const addDays: (date: PossibleConstructors, days?: number) => Date;
