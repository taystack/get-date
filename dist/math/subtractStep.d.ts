import { Step, PossibleConstructors } from '../types';
/**
 * short-hand for addStep(date, Step, -count)
 */
export declare const subtractStep: (date: PossibleConstructors, step?: Step, count?: number) => Date;
